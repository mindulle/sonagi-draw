class MicrotaskNotifier {
  listeners = /* @__PURE__ */ new Set();
  notify(...props) {
    queueMicrotask(() => {
      for (const listener of this.listeners) {
        try {
          listener(...props);
        } catch (error) {
          console.error("Error in MicrotaskNotifier listener", error);
        }
      }
    });
  }
  register(_listener) {
    let didDelete = false;
    queueMicrotask(() => {
      if (didDelete) return;
      this.listeners.add(_listener);
    });
    return () => {
      if (didDelete) return;
      didDelete = true;
      this.listeners.delete(_listener);
    };
  }
}
export {
  MicrotaskNotifier
};
//# sourceMappingURL=MicrotaskNotifier.mjs.map
