"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var recordDiff_exports = {};
__export(recordDiff_exports, {
  applyAndDiffRecord: () => applyAndDiffRecord,
  diffAndValidateRecord: () => diffAndValidateRecord,
  validateRecord: () => validateRecord
});
module.exports = __toCommonJS(recordDiff_exports);
var import_diff = require("./diff");
var import_TLSyncClient = require("./TLSyncClient");
function diffAndValidateRecord(prevState, newState, recordType, legacyAppendMode = false) {
  const diff = (0, import_diff.diffRecord)(prevState, newState, legacyAppendMode);
  if (!diff) return;
  try {
    recordType.validate(newState);
  } catch (error) {
    throw new import_TLSyncClient.TLSyncError(error.message, import_TLSyncClient.TLSyncErrorCloseEventReason.INVALID_RECORD);
  }
  return diff;
}
function applyAndDiffRecord(prevState, diff, recordType, legacyAppendMode = false) {
  const newState = (0, import_diff.applyObjectDiff)(prevState, diff);
  if (newState === prevState) return;
  const actualDiff = diffAndValidateRecord(prevState, newState, recordType, legacyAppendMode);
  if (!actualDiff) return;
  return [actualDiff, newState];
}
function validateRecord(state, recordType) {
  try {
    recordType.validate(state);
  } catch (error) {
    throw new import_TLSyncClient.TLSyncError(error.message, import_TLSyncClient.TLSyncErrorCloseEventReason.INVALID_RECORD);
  }
}
//# sourceMappingURL=recordDiff.js.map
