class NodeSqliteWrapper {
  constructor(db, config) {
    this.db = db;
    this.config = config;
  }
  db;
  config;
  exec(sql) {
    this.db.exec(sql);
  }
  prepare(sql) {
    return this.db.prepare(sql);
  }
  transaction(callback) {
    this.db.exec("BEGIN");
    let result;
    try {
      result = callback();
    } catch (e) {
      this.db.exec("ROLLBACK");
      throw e;
    }
    this.db.exec("COMMIT");
    return result;
  }
}
export {
  NodeSqliteWrapper
};
//# sourceMappingURL=NodeSqliteWrapper.mjs.map
